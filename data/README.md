# Data Layout

- `raw/`: original dataset files exactly as received
- `interim/`: merged or lightly cleaned working tables
- `processed/`: modeling-ready datasets and scoring inputs

Do not overwrite `raw/` files after download.

Recommended location for the synced Statlog source files:

- `data/raw/statlog_german_credit/`
